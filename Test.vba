Public const Ciao = "ciao"
Dim TestRange As Range

For i = 0 to 5
  debug.print ("printa questo")

  debub.Print("test copy paste")
  debug.Print ("shift")
  debug.print ("other

  Range = Worksheets("Test").Cells("A1")
Next i

While i < 6
  'Do nothing

Loop
